# 灵云01号伴飞电脑开发指南

> 本目录包含面向伴飞电脑(Raspberry Pi 5)设备监控开发的全部技术文档。
> 当前阶段: **Phase-1 设备监控**(BMS / MPPT / DCDC), 暂不接入飞控。

## 系统定位

伴飞电脑(Raspberry Pi 5)作为**机载数据中继节点**:

```
  MPPT ──┐
  BMS  ──┼── CAN总线(SocketCAN) ──► 树莓派5 解析/整理 ──► 串口数传 ──► Qt上位机(地面)
  DCDC ──┘       (250kbps, 扩展帧)   (airship_* 节点)      (JSON帧)
```

- 接收各电源设备(MPPT/BMS/DCDC)的 CAN 帧
- 解析厂家协议为结构化数据
- 经串口数传统一下传给地面 Qt 上位机

## 文档列表

| 文档 | 内容 | 适用场景 |
|------|------|---------|
| [01_设备监控架构](./01_设备监控架构.md) | 树莓派5+ROS2 Jazzy 设备监控软硬件架构、CAN 接入、节点拓扑、消息定义、数传协议 | 机载开发/架构理解 |
| [02_CAN协议汇总](./02_CAN协议汇总.md) | BMS/MPPT/DCDC 厂家 CAN 协议映射、帧 ID、信号分辨率速查 | 驱动开发/调试 |
| 厂家协议原文 | [lingyun01/](./lingyun01/) 目录下各设备厂家协议文件 | 协议核对 |

## 快速上手

### 环境

- 树莓派5 + Ubuntu 24.04 + ROS2 Jazzy
- CAN 接入: USB-CAN 适配器, SocketCAN 接口 `can0`(250kbps)

### CAN 接口初始化

```bash
sudo ip link set can0 type can bitrate 250000
sudo ip link set can0 up
# 验证
ip -details link show can0
candump can0
```

### 构建与启动

```bash
source /opt/ros/jazzy/setup.bash
colcon build
source install/setup.bash

# 启动全部设备监控节点
ros2 launch airship_bringup device_monitor.launch.py

# 查看设备状态
ros2 topic echo /bms/status
ros2 topic echo /mppt/status
ros2 topic echo /dcdc/status
ros2 topic echo /monitor/device_alert
```

### 测试

```bash
colcon test
colcon test-result --verbose
```

## 版本信息

- **生成日期**: 2026-08-05
- **ROS2 版本**: Jazzy Jalisco (LTS)
- **目标硬件**: Raspberry Pi 5 (8GB) + USB-CAN 适配器
- **当前阶段**: Phase-1 设备监控 (BMS/MPPT/DCDC)
- **后续阶段**: Phase-2 接入飞控与飞行控制